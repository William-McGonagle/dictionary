export const metadata = {
  title: {
    default: "Lost",
    absolute: "Words - GUSD",
    template: "%s - Definition - GUSD",
  },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
